#include <stdio.h>

/* this program takes in 0 command line arguments which must be integers and prints the sum of them.
 */

int main( int argc, char *argv[] )  {
     int A = 6;
     int B = 8;
     printf(" A + B is %d \n", A + B);
}
